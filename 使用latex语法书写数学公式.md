## 使用latex语法编写下面这行公式
![](images/image1.png)
## 在markdown文件中书写公式需要使用"$"符号包裹公式，公式单独成行并居中显示需使用两个'$'符号包裹，公式在行内字段内显示使用一个'$'符号包裹并使用空格间隔
## 公式书写如下('单引号是为了使公式能显示出来,当$符号前面为空格时公式才能正常显示）：
'$$J(\theta_{0}，\theta_{1})= \frac{1}{2m}\sum_{i=1}^{m}(h_\theta(x^{i})-y^{i})^{2} $$
## 公式单独成行展示如下：
'$$J(\theta_{0},\theta_{1})= \frac{1}{2m}\sum_{i=1}^{m}(h_\theta(x^{i})-y^{i})^{2}$$

$$J(\theta_{0},\theta_{1})= \frac{1}{2m}\sum_{i=1}^{m}(h_\theta(x^{i})-y^{i})^{2}$$
## 公式行内显示展示如下：
这是一个公式（注意这里$与前面没有空格，所以公式没有直接展示出来，显示的是源码）$J(\theta_{0},\theta_{1})= \frac{1}{2m}\sum_{i=1}^{m}(h_\theta(x^{i})-y^{i})^{2}$这是一个公式

这是一个公式 $J(\theta_{0},\theta_{1})= \frac{1}{2m}\sum_{i=1}^{m}(h_\theta(x^{i})-y^{i})^{2}$这是一个公式
### 具体的latex语法及希腊字母发音文件已上传到本库中images文件夹中需要自取
